# Copy to .env. Committed values are local-only throwaways.
# Port 5433, not 5432, so the container does not collide with a Postgres
# a reviewer may already have running locally.
DATABASE_URL=postgres://app:app@localhost:5433/orders
JWT_SECRET=dev-only-not-a-real-secret
PORT=3000
